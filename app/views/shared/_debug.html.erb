<% debug(params) if Rails.env.development? %>
<% debug(request.env) if Rails.env.development? %>
<% debug(Time.now) if Rails.env.development? %>
<% debug(session) if Rails.env.development? %>
<% debug(@sc) if Rails.env.development? %>