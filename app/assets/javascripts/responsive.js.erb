$( function() {
  if ( $( '#site .signUpForm' ).length ) {
    $( '#site' ).css( 'width', 960 ).css( 'margin-top', -16 );
    $( 'header.row-fluid.navbar.navbar-fixed-top' ).removeClass( 'navbar-fixed-top' );
    $( '#content_area' ).css( 'margin-top', 34 );
    $( 'header .container-fluid' ).css( 'padding-left', 0 ).css( 'padding-right', 0 );
    $( '#logo h2' ).css( 'margin-left', 0 );
  }
} );